import type { SessionRecord, TaskType } from '../shared/types'
export function formatDuration(seconds: number): string { if (seconds < 60) return `${Math.max(0, Math.round(seconds))}s`; const minutes = Math.floor(seconds / 60); const remaining = Math.round(seconds % 60); if (minutes < 60) return remaining ? `${minutes}m ${remaining}s` : `${minutes}m`; return `${Math.floor(minutes / 60)}h ${minutes % 60}m` }
export function formatDate(value: string): string { return new Intl.DateTimeFormat(undefined, { month: 'short', day: 'numeric', year: 'numeric' }).format(new Date(value)) }
export function formatTime(value: string): string { return new Intl.DateTimeFormat(undefined, { hour: 'numeric', minute: '2-digit' }).format(new Date(value)) }
export function taskTypeLabel(value: TaskType): string { return { writing_creating: 'Writing or creating', coding_problem_solving: 'Coding or problem solving', reading_research: 'Reading or research', learning_tutorial: 'Learning or tutorial', communication_coordination: 'Communication or coordination', other_planned_task: 'Other planned task' }[value] }
export function sessionOutcome(session: Pick<SessionRecord, 'driftLabel' | 'postSessionAnswer' | 'status'>): string { if (session.driftLabel === 1) return 'Drift'; if (session.driftLabel === 0) return 'Aligned'; if (session.postSessionAnswer === 'not_sure') return 'Not sure'; if (session.status === 'pending_reflection') return 'Reflection pending'; if (session.status === 'active') return 'Active'; return 'Unlabeled' }
